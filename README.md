# Registry-Loader.ts
A deterministic registry loader that retrieves module metadata, workflow definitions, capability maps, dependency edges, and phase bindings from the unified system registry. It prepares the orchestration engine with validated, structured configuration for sovereign, autonomous Beast System 3.0 execution.
